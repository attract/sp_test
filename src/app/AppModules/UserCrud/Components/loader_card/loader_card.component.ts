import { Component, ViewChild, Input, Output, EventEmitter } from '@angular/core';
import { LIMIT_ON_PAGE } from '../../../../constants';

@Component({
  selector : 'loader-card',
  templateUrl : 'loader_card.component.html',
  styleUrls : ['loader_card.component.less']
})

export class LoaderCardComponent {

    constructor() {}
}
